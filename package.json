{
  "name": "cjdb_crawler",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "typecheck": "wxt prepare && vue-tsc --noEmit",
    "test": "npm run typecheck && npm run build && playwright test",
    "test:unit": "playwright test tests/unit",
    "test:browser": "npm run build && playwright test tests/browser"
  },
  "dependencies": {
    "chrono-node": "^2.9.0",
    "jszip": "^3.10.1",
    "turndown": "^7.2.2",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/chrome": "^0.0.253",
    "@types/turndown": "^5.0.6",
    "@wxt-dev/module-vue": "^1.0.0",
    "prettier": "^3.9.6",
    "sass": "^1.69.0",
    "typescript": "^5.3.0",
    "vue-tsc": "^3.3.11",
    "wxt": "^0.20.0"
  }
}
